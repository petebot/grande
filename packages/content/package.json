{
  "name": "@grande/content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "check": "tsc --project tsconfig.json --noEmit && tsc --project tsconfig.test.json",
    "snapshot": "tsx ../../scripts/generate-content-snapshot.ts",
    "snapshot:validate": "vitest run tests/snapshot.test.ts",
    "test:unit": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "groq-js": "^1.30.3",
    "typescript": "^6.0.3"
  }
}
